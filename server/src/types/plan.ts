/** A user's trading plan data for a specific space. */
export interface PlanData {
  checklist: string[];
  coreRules: string[];
  tradingSetup: string[];
  mistakes: string[];
  identity: string[];
}
